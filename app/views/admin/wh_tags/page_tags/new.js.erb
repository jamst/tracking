$('#page_tag_modal').remove();
$('body').append('<div class="ui modal" id="page_tag_modal"><i class="close icon"></i><h2 class="ui dividing header">添加标签</h2><%= j(render 'new') %></div>');
$('#page_tag_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();


// TODO表单校验
$("#commition").click(function () {
    if ($("#page_tag_page_url").val() != "") {
        $("#new_page_tag").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});
