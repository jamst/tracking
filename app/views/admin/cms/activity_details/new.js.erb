$('#new_activity_details_modal').remove();
$('body').append('<div class="ui modal" id="new_activity_details_modal"><i class="close icon"></i><h2 class="ui dividing header">添加活动规则</h2><%= j(render 'new') %></div>');
$('#new_activity_details_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();


// TODO表单校验
$("#commition").click(function () {
    if ($("#activity_details").val() != "") {
        $("#new_activity_detail").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});
