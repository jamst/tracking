$('#activity_modal').remove();
$('body').append('<div class="ui modal" id="activity_modal"><i class="close icon"></i><h2 class="ui dividing header">添加新活动</h2><%= j(render 'new') %></div>');
$('#activity_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown();
$('#calendar1').calendar({type: 'date'});
$('#calendar2').calendar({type: 'date'});


// TODO表单校验
$("#commition").click(function () {
    if ($("#activity_name").val() != "") {
        $("#new_activity").submit();
    } else {
        $("#form_message").text("请完善信息！").show(200);
    }
});
